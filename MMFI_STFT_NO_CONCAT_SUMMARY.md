# MMFI STFT训练（无时域拼接版本）

## 完成时间
2025-11-22 16:35

## 关键变更

### 1. 移除时域拼接
**之前（有拼接）**:
```python
# 拼接5个样本: T=10 → T=50
concat_num = 5
stft_processor = CSISTFTProcessor(
    nperseg=16,         # 适合T=50
    noverlap=8,         # 50%重叠
    nfft=32,
    output_size=(224, 224),
    use_log_scale=True
)
```

**现在（无拼接）**:
```python
# 直接使用原始样本: T=10
stft_processor = CSISTFTProcessor(
    nperseg=8,          # 适合T=10
    noverlap=6,         # 75%重叠
    nfft=16,
    output_size=(224, 224),
    use_log_scale=True
)
```

### 2. 数据集规模变化

| 指标 | 拼接版本 | 无拼接版本 | 变化 |
|-----|---------|----------|------|
| 原始样本 | 2700 (27×100) | 2700 (27×100) | 不变 |
| 拼接后样本 | 540 (2700/5) | 2700 (不拼接) | **5倍** |
| 训练集 | 432 | 2160 | **5倍** |
| 测试集 | 108 | 540 | **5倍** |
| 每类训练样本 | 16 | 80 | **5倍** |

### 3. STFT参数优化

**优化点**:
- `nperseg`: 16 → 8 (适应T=10)
- `noverlap`: 8 → 6 (50% → 75%重叠)
- `nfft`: 32 → 16 (降低频率分辨率，但足够T=10)

**优势**:
- 75%重叠提高时频分辨率
- 更适合短时间序列（T=10）
- 避免了拼接带来的语义不连续问题

## 数据验证

### 文件统计
```bash
训练集样本: 2160 个 PNG 图像
测试集样本: 540 个 PNG 图像
总计: 2700 个样本 (27类 × 100样本)
```

### 标签分布
```
训练集标签分布: 每类 80 个样本
测试集标签分布: 每类 20 个样本
80/20 划分比例，符合预期
```

## 为什么去掉拼接？

### 1. 语义连续性问题
**拼接版本的问题**:
```
原始数据: [样本1, 样本2, ..., 样本2700] (来自27个不同动作类别)
↓ shuffle后
[样本A(人17), 样本B(人5), 样本C(人33), 样本D(人22), 样本E(人9), ...]
↓ 拼接5个样本
拼接样本1 = [A, B, C, D, E] → 时间上不连续，来自不同人！
```

**问题严重性**:
- 拼接的样本在时间维度上完全不连续
- 混合了不同人的CSI数据
- STFT会产生虚假的时频特征
- 模型学到的是错误的模式

### 2. 数据量不足
- 拼接后只有432个训练样本
- 27个类别，每类平均只有16个样本
- 对于109M参数的ViT模型，严重过拟合

### 3. 与Wiar对比
**Wiar STFT配置**:
- 16个类别
- 使用原生时间长度（T较大）
- 没有拼接
- STFT参数: nperseg=20, noverlap=10, nfft=32

**MMFI应该怎么做**:
- 27个类别
- 使用原生T=10
- 不拼接
- STFT参数优化: nperseg=8, noverlap=6, nfft=16

## 当前状态

### 数据生成
✅ **已完成** - 2160训练 + 540测试

### 批量训练
🔄 **进行中** - 100个随机种子训练
- 开始时间: 2025-11-22 16:35
- 预计完成: 2025-11-23 早晨
- 进度监控: `tail -f batch_mmfi_stft.log`

### 配置文件
- 数据集: `mmfi_stft`
- 配置: `configs/mmfi_short_stft.json`
- 任务划分: 12初始 + 5×3增量 = 6个会话

## 预期结果

### 相比拼接版本
- ✅ 训练数据增加5倍 (432 → 2160)
- ✅ 每类样本从16增加到80
- ✅ 避免了语义不连续问题
- ✅ STFT参数更适合T=10
- ✅ 与Wiar方法论一致

### 预期准确率
基于以下假设:
- Wiar (16类, 类似数据规模): ~60-70%
- MMFI (27类, 难度更高): **预期 50-60%**
- 相比by_person (10-18%): **显著提升**

## 技术细节

### 代码修改位置
`utils/data.py` 第679-779行 `iMMFIDataSTFT`类:
- 移除`concat_num`变量
- 移除拼接逻辑（713-738行）
- 更新STFT参数
- 直接使用原始样本: `labels = np.ones(len(data)) * (i-1)`

### 日志输出变化
```python
# 之前
print(f"[时域拼接] 拼接{concat_num}个样本: T=10 → T=50")

# 现在
print(f"[无拼接] 使用原始T=10数据，不进行时域拼接")
```

## 监控命令

```bash
# 监控批量训练进度
tail -f batch_mmfi_stft.log

# 查看最新完成的训练
ls -lt logs/mmfi_stft/12_3_sip/lorasub_drs/Adam/ | head -10

# 统计已完成的训练数
ls logs/mmfi_stft/12_3_sip/lorasub_drs/Adam/*.log | wc -l

# 查看特定种子的结果
tail logs/mmfi_stft/12_3_sip/lorasub_drs/Adam/1824.log
```

## 下一步

1. ⏳ 等待100次训练完成
2. 📊 分析结果，计算Top5平均准确率
3. 📈 与by_person方案对比
4. 📝 总结最优配置

---

**核心结论**: 通过移除时域拼接并优化STFT参数，我们获得了5倍的训练数据量，并避免了语义不连续的问题。这应该显著提升MMFI的增量学习性能。
